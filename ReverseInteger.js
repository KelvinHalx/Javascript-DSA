// Algorithm to reverse an integer
I
// 1. convert to a string
// 2. turn into array
// 3. reverse method
// 4. back to string
// 5. back to int

const reverseInt = (n)=> {
    return parseInt(n.toString().split('').reverse().join(''))
} 

console.log({
    
})

